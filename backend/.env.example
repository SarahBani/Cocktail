# Copy this file to .env and fill in the values for your local environment.
# Docker: these values are overridden by the `environment:` block in docker-compose.yml.
# Production: inject secrets via your hosting platform (e.g. AWS Secrets Manager, K8s secrets).

PORT=
DATABASE_URL=
ELASTICSEARCH_HOST=
